---
- name: Template Playbook
  hosts: servers
  become: yes
  gather_facts: yes
  tasks:
    - name: First step
      ansible.builtin.ping:
    
    - name: Second step
      ansible.builtin.include_role:
        name: template-role
...